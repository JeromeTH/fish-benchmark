method: random  # or grid or bayes
metric:
  name: val_loss
  goal: minimize
parameters:
  learning_rate:
    min: 1e-5
    max: 1e-2
  optimizer:
    values: ["adam", "adamw", "sgd"]
  batch_size:
    values: [16, 32, 64]
  epochs:
    value: 20
